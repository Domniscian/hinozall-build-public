# Hinozall public build runner

Temporary public CI workspace. **No private signing key, keystore, password, Instagram credential, cookie or session is stored here.** The public CI only compiles an unsigned APK; final signing is done outside this repository.
